function ret = farthest_point_clustering( X, m )
d = size(X, 1);
n = size(X, 2);
ret = X(1:end, 1:m);
for i = 2:m
  dist = inf;
  for j = 1:(i - 1)
    dist = min(dist, measure(log_map(X(1:end, i), X(1:end, 1)), ...
                             X(1:end, 1)));
  end
end